package com.shopease.model;

/**
 * Enumeration representing user roles in the ShopEase multi-vendor marketplace.
 * Role-Based Access Control (RBAC) relies on these identifiers for authorization checks.
 */
public enum Role {
    BUYER,
    SELLER,
    ADMIN
}
